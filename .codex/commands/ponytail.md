---
description: Apply ponytail intensity to the current request
codex-trigger: "@ponytail"
claude-equivalent: "/ponytail"
---

Apply ponytail $ARGUMENTS to the current request only. If no level is specified, use full; `off` leaves the request in normal mode. Before any code: does it need to exist at all (YAGNI)? Does the standard library do it? A native platform feature? Can it be one line? Build the minimum that works. No unrequested abstractions, no avoidable dependencies, no boilerplate. Mark intentional simplifications with a `ponytail:` comment.
