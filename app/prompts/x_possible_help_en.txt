Show the most recent Possible-run ideas generated by `ft possible run`.

Usage: /x_possible

The bot reads the newest `ideas/*.json` file produced by the host-side
`ft` binary and displays the top idea nodes. The bot itself does not
run idea generation; trigger it on the host with:

    ft possible run --defaults --background

If no ideas file exists yet, the command returns a friendly hint.
